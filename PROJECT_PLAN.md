# 项目实施方案

## 当前目标

首个里程碑是一个可验证的英语句级复述系统：以 CMU ARCTIC 多名真人为输入，以一名固定、已授权说话人为训练目标。模型支持任意长度 Mel 分块，并保证整段与分块推理一致。

当前代码属于声学基线，不把时轴插值误认为严格语音对齐，也不把 Griffin-Lim 诊断音频误认为最终声码器质量。

## 数据路线

1. CMU ARCTIC：主平行数据，同一 utterance ID 跨说话人配对。
2. ESD：第二阶段加入中英文、说话人和情绪变化。
3. VCTK：用于非平行预训练和未见说话人泛化。
4. DAPS/噪声增强：用于真实麦克风鲁棒性。

训练/验证按 utterance ID 划分，禁止同一句文本跨集合泄漏。正式发布前必须再次核对各数据集和目标音色的许可。

## 模型路线

当前版本使用 16 层因果门控 Token 管线。新 Mel 帧仅进入第一层，深层只能读取上一时刻的前一层状态。后续阶段依次加入：

1. 冻结的 HuBERT/WavLM 内容编码器；
2. F0、能量、停顿等独立韵律通道；
3. MFA 音素对齐或 Soft-DTW；
4. 固定目标音色的神经声码器；
5. 句尾 VAD 与 listen-then-repeat 状态机；
6. 通过对照实验验证后再启用固定容量联想记忆。

## 验收门槛

- 单元测试：未来扰动不影响过去，任意分块等价；
- 声学：验证集 Mel MAE 相对“复制输入 Mel”基线明显下降；
- 内容：使用独立 ASR 计算 WER/CER；
- 身份：输出接近固定目标音色且远离输入说话人；
- 工程：T4 上实时率低于 1，分块最大数值误差接近浮点零。

