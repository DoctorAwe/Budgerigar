# 鹦鹉模型

## 核心行为是模仿鹦鹉学舌的行为,针对输入的语音流以模型的思考方式

- 参考AutoMachine的模型结构和设计思路
- 先寻找可靠的数据集,即同语句不同音色(最好是两个不同的真人真声)的数据集
- 核心任务是,模型倾听任何可能的音色输入，然后按照自己的音色和语调进行复
- 模型应用场景是实时的音频输入流，然后实时输出音频流
- 注意，多层token序列并不是只能保留tick*层数内的输入内容，而是会根据每层的优化器不断提取关键特征从而保留较长时间内的信息
- 注意，真实验证环境以及数据集下载均在colab中进行
- 编写colab里部署与训练的文档，并使用gradio构建演示页面