# 神经处理机

## 这是一个基于transformer的流式输入输出模型

- 基于现在LLM的基础架构，处理信号的基础架构沿自自注意力机制
- 处理流式信号(输入/输出), 时域信号, 如视频流, 音频流等其他信号流, 输出也是时域信号, 如控制电信号流等
- 核心任务是 同步感知信号 → 同步响应/控制信号, 即 刺激 -> 响应
- 核心架构设想: 
先将输入时域信号通过一个不太复杂的神经网络编码器进行压缩编码, 方法是这样的, 编码器有固定属性输出尺寸和频率, 接受的时域信号将被编码成这个尺寸和频率进行模型内部的输入, 如160:90 180hz 的视频流将被编码成 100*100 60hz 的三通道输入网络, 其中原始输入的每三帧将被编码成新输入的一帧, 这一步只是在限制输入流量以防计算能力不足

模型内部通过提前设置的多层token序列, 输入按照时间顺序一层一层的往下走, 并且每走一步都会被这一层的优化器和当前token层状态融合, 比如 现在有9层顺序的token层, 第一个编码帧, 编码后的信号到第一层, 然后被这一次的优化器融合了信号，第二个编码帧,当前层状态流动到第二个token层，然后再次被优化器整合， 新输入的信号流入第一层，之后同理, 并且token层的大小并不一定保证完全一致, 比如可以吧最末尾的几层设置的较大,模拟长期记忆

token 层不是“一个层只保存一个时间片或一个单词”的缓存，也不应仅被理解为层数越多、固定窗口越长。每一层都保存该层 optimizer 对此前输入形成的当前理解：新证据到来时，旧理解会被保留、修订、重组或遗忘。靠后的层接收前层已经提炼过的状态，并在更慢的时间尺度上反复优化，因此可以逐渐形成对完整短语、整句语义、语气与长期上下文的整体理解。注意力层负责读取和组合这些不同抽象尺度的理解，但真正的处理同时发生在每层 optimizer 内部。

精确的音素顺序和复读时间细节应由独立的有序事件带保存；层级 token 保存“对这些事件的理解”。二者不能混为简单 FIFO：事件带回答“接下来具体发什么音”，后层 token 回答“整句话表达什么、前后关系是什么、应该以什么语气复读”。

结构如图示：

```text
                      [OUT]
                        |
                    [DECODER] 
                        ^
                        | (ATTEION)
            |------------------------|
 [INPUT] -> []->[]->[]->[]->[]->[]->[]
```

# AutoMachine 思想在 Budgerigar 中的映射

下述内容把上面的原始设想约束成可实现、可验证的 Budgerigar 方案。基础版本保持“状态随 tick 逐层迁移，并在每层与新信息融合”；多速率层是后续可选优化，不替代基础版本。

## 核心抽象

模型以固定长度的音频 tick 工作。新声学 token 进入第 0 层，旧状态向下一层迁移；每层通过独立或共享的可学习更新器，把迁入状态、该层旧状态和当前可见输入融合成新状态。后续可把若干深层组成更新较慢、容量更大的长期记忆组。

因此，第 `l` 层不是仅保存最近 `tick × 层数` 的原始输入，而是保存一个容量有限、时间跨度可增长的充分统计量。状态会遗忘细节，但可长期保留说话阶段、语义主题、语速趋势和韵律轮廓等关键特征。

形式化表示：

```text
x_t = ContentEncoder(audio_tick_t)
h_t^0 = U_0(h_{t-1}^0, x_t)
h_t^l = U_l(h_{t-1}^l, h_{t-1}^{l-1}, x_t)
y_t = Decoder(x_t, Read({h_t^l}), budgerigar_voice)
```

其中 `U_l` 是每层的状态优化器，`Read` 是对当前各层 token 的因果注意力读取。建议第一版使用门控 MLP/GRU 实现 `U_l`，使用小型因果 Transformer 实现 `Read`；稳定后再尝试共享优化器、多速率更新或状态空间模块。

## 与普通缓存的区别

| 项目 | 滑动窗口缓存 | 层级状态 |
|---|---|---|
| 保存内容 | 最近若干帧 | 学习得到的摘要 |
| 时间跨度 | 固定 | 理论上持续，但信息逐渐压缩 |
| 内存开销 | 随窗口增长 | 固定 |
| 风险 | 长期上下文丢失 | 状态污染、过度遗忘 |

## 必须验证的假设

1. 长句后半段的一致性优于同参数量短窗口基线。
2. 重置高层状态会显著影响长期韵律，但不应破坏当前音素内容。
3. 状态中不能高精度恢复输入说话人身份，否则存在音色泄漏。
4. 断句或长静音时需要软衰减，而不是无条件清空所有层。
5. 不提供离散“监听/结束/朗读”状态时，层级状态仍能从连续训练 episode 中学会先积累完整表达、再生成复读。
6. 改变句内停顿长度不应机械触发提前复读；模型需要利用内容与长期上下文共同判断完整性。
