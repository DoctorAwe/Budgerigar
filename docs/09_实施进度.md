# 实施进度

更新日期：2026-08-05。

## Colab 真实验证记录

2026-08-05 已完成第一轮数据链路：

- 环境：Google Colab，Tesla T4 15 GB。
- Python：3.12.13；PyTorch：2.11.0+cu128；CUDA：12.8。
- 数据来源：CMU ARCTIC 官方 Festvox 归档。
- manifest SHA-256：`15ee6046e8d2dff999b52887c86e9109dd6ae7631a69f5ca2ba4a9588bc6061e`。
- manifest 与运行元数据已保存到用户 Google Drive。
- 首次元数据中的 `git_commit` 为空，已修复自动仓库定位；下次运行需确认 commit 与 `git_dirty` 字段。

该记录证明真实下载、索引、审计和 Drive 持久化链路可运行，不代表模型训练或音频质量已通过验收。

## 环境约束

- 本地仅进行实现、静态检查和无真实数据的轻量单元测试。
- 数据集下载、真实数据审计、GPU 训练、音频质量和实时性能验证均在 Colab 进行。
- 当前本地冒烟测试只证明接口能运行，不代表任何模型或数据阶段已经通过真实验收。

## 已完成

### 阶段 0 基础约束

- 固定内部采样率、模型 tick 与 look-ahead 配置。
- 建立 session 级音频缓冲，支持不规则输入、flush 和 reset。
- `algorithmic_buffer_ms` 明确报告 tick 与 look-ahead 的基础缓冲；完整端到端延迟仍需在模型接入后测量。

### 阶段 1 数据入口

- 实现设计文档所述 JSONL manifest 的严格解析。
- 实现重复 ID、非法 split、缺失音频、WAV 时长偏差检查。
- 实现 `parallel_id` 跨 split 泄漏检测、文本泄漏警告和真人多音色平行组统计。
- 提供 `budgerigar-audit` 命令行入口。
- 实现 CMU ARCTIC 官方归档的幂等下载、SHA-256 校验和安全解压。
- 实现 CMU ARCTIC 与已取得官方 ESD 数据的统一 manifest 适配器。
- 使用稳定哈希按 `parallel_id` 切分，避免同一平行语句跨集合泄漏。
- 提供 [Colab 数据 notebook](../notebooks/Budgerigar_Data.ipynb)，包含许可确认、Drive 缓存、真实审计、报告与运行元数据保存。

### 验证

- 已加入音频分块与 manifest 自动化测试。
- 当前绑定运行时未安装 pytest，因此正式 pytest 套件尚未执行。
- 已通过标准库冒烟测试与 `compileall` 语法检查。
- CMU ARCTIC 适配器已使用合成目录结构完成配对与审计冒烟测试；真实下载仍待 Colab 执行。
- Colab 真实下载、manifest 审计和 Drive 保存已完成；阶段 1 数据链路通过，详细样本统计以 Drive 中 `data_audit.json` 为准。

### 阶段 2 特征入口

- 实现 24 kHz、100 维 log-Mel、10 ms hop 的特征提取。
- 使用 `center=False`，避免特征提取隐式读取整句未来上下文。
- 实现 RMS 增益限制、削波统计、帧能量和相对阈值 VAD。
- 特征配置生成稳定 fingerprint；缓存文件与输出 manifest 绑定该版本。
- 支持 Colab/Drive 中断恢复和原子缓存写入。
- 提供 [特征 notebook](../notebooks/Budgerigar_Features.ipynb)及随机样本可视化。
- 当前状态为“实现完成，等待 Colab 真实特征报告与图像检查”。

## 下一步

1. 在 Colab 执行特征 notebook，检查特征报告及随机 Mel/VAD 图。
2. 根据真实报告修正异常音频、增益和 VAD 阈值。
3. 实现平行特征数据集与训练用连续随机切块。
4. 建立训练 smoke notebook，实现固定 Budgerigar 目标身份的离线 any-to-one 最小模型。
5. 在 Colab 验证离线基线后，实现逐 tick 多层 token 状态和层级探针测试。

## 当前不包含

- 尚未下载数据集，也没有把未经确认许可的数据加入仓库。
- 尚未生成或训练模型 checkpoint。
- 尚未实现声码器、实时麦克风输出、Colab notebook 或 Gradio 页面。
