# 实施进度

## 2026-08-06 主线重置

项目主线已调整为“先完整复读输入自身，再转换音色，最后加入人格语调”。当前所有 `arctic_slt` 目标声线端到端实验停止扩训，checkpoint 保留为失败对照。新路线和严格验收标准见 [阶段重置：先完整复读，再转换音色与语调](11_阶段重置_先完整复读再转换音色.md)。下一步先在 Colab 建立可重建波形的 codec-token 缓存和同音延迟复读数据集。

- 第一版 codec 选择 Meta EnCodec 24 kHz mono、6 kbps，与现有 24 kHz 数据一致。
- 已实现 codec fingerprint、token payload、codec manifest 和波形解码函数。
- Colab 入口 `notebooks/Budgerigar_Codec_Copy_Data.ipynb`：先编码/试听 8 条 smoke，人工确认后才允许全量提取。
- EnCodec 全量提取完成：2263 条，fingerprint `e47d29bb8ba86e3e`。
- 已建立同音自复读 codec episode：输入与目标 token 完全一致，仅插入 160–280 ms 思考间隔；新 Colab `Budgerigar_Codec_Copy_Episodes.ipynb` 先审计码本、帧率、token 范围与时间轴，再进入模型训练。
- Drive 上逐个打开 2263 个小 payload 会导致全量审计超时；改为全量 manifest/fingerprint 检查加 64 条均匀 payload 深度抽样。8 条 episode 已确认 8 码本、约 75 Hz、13–21 帧思考间隔且 exact token copy 为真。
- 64 条均匀审计通过：码本数 8、token `0–1023`、帧率 `75.04–75.44 Hz`，fingerprint 一致。
- 已实现第一版 codec token-tape 模型与 `Budgerigar_Codec_Copy_Train.ipynb`：tape 保存原 token，4 层递归理解只读，高层不允许改写内容；连续 read phase 输出 8 个码本。首轮 200 steps，门槛 token accuracy `>99%`、整句完全正确率 `>90%`、提前发声率 `<1%`。
- 首轮在 step 128 停止：validation token accuracy `2.988%`、整句完全正确率 `0%`、提前发声率 `1.688%`。分类头重新猜测 tape 中已有 token，成为不必要瓶颈。
- 修正版由 tape 注意力直接构造 1024 类 token 分布，神经网络只学习 readiness 与单调读指针；新增复读发声召回指标，独立目录 `codec_copy_direct_tape_*`。
- Direct-tape step 100：token accuracy `2.759%`、exact utterance `0%`、early voice `1.201%`、repeat voice recall `99.203%`。发声正常但 token loss 停在随机交叉熵 `6.93`，指针未对齐。
- 原 clock loss 相对 token loss 权重不足，且指针远离正确位置时窄窗口产生梯度下溢。下一轮仅将 clock weight 提至 `50`、限制 100 steps，并新增 validation read-phase MAE（codec 帧）；独立目录 `codec_copy_direct_tape_clock50_*`。
- 整数寻址直接回放实验仅作为已否决对照：它证明了时序控制可以收敛，但属于程序化复制，且验证集因地址漂移仍然失败。正式路线已移除不可修改 tape、直接返回输入 token、固定速率和显式读指针。
- 当前结构为完全可学习复读网络：流式递归编码器将已听音频压缩为多层隐状态，解码器通过软注意力回忆这些学习表征，并依靠自身上一帧输出自回归生成全部 EnCodec token。结束判断、思考间隔、发声概率与复读内容均由网络学习。
- 完全可学习 token 模型首轮验证仍失败：step 128 的 token accuracy 为 `1.6737%`、exact utterance 为 `0%`；时序发声召回达到 `99.68%`，但内容只学到 codec 统计分布。停止扩训并舍弃离散 token 作为主任务表示。
- 主线重构为连续潜空间神经复读：因果波形编码器、上下文化连续潜序列、多尺度整体理解状态、无硬指针软注意力、自回归连续声学生成和神经波形解码。详细阶段见 [连续潜空间神经复读重构](12_连续潜空间神经复读重构.md)。
- 已实现 R0 独立连续波形自编码器与 Colab smoke notebook。它直接读取原始 WAV，以因果卷积压缩到约 75 Hz 连续潜空间，再由神经解码器生成 24 kHz 波形；训练报告多分辨率 STFT 和 SI-SDR，并保存输入/重建试听样本。
- 研究尺度再次收缩到单发音短时记忆：先采用 FSDD 的 0–9 短数字发音训练流式延迟 token 输出，通过后再生成当前输入的原音调短音频。新数据 notebook 为 `Budgerigar_FSDD_Data.ipynb`，详细阶段见 [单发音短时记忆路线](13_单发音短时记忆路线.md)。长语音 R0 暂停但代码保留。
- FSDD 审计通过：3,000 条、6 位说话人、8 kHz，时长 `0.1435–2.28275 s`。S0 已实现任意外部 chunk 缓冲、连续听觉前端、三层 GRU 短时记忆与延迟数字 token 输出；Colab 入口为 `Budgerigar_Short_Memory_S0.ipynb`。
- S0 输入前端采用耳蜗式近似：32 个对数频率因果带通通道保存跨 chunk FIR 历史，响应经过整流、幂律压缩、2.5 ms 包络与变化率编码。高层每 10 ms 更新一次，批量训练和逐 tick 推理共享相同滤波与 GRU 状态，并检查 logits 数值一致性。
- 根据短时理解需求，S0 高层已由三层 GRU 改为 16 层递归 token 状态：每层 96 维，层间共享注意力、门控更新器和精炼 MLP，各层保留独立状态与层级 embedding。输出训练改用 120–280 ms 窗口 CTC，并增加保持区间数字内容辅助头，移除不可预测的单点随机发射监督。
- 16 层版本 step 252 仍停在随机内容基线：validation memory accuracy `10%`、emission recall `0%`，content loss 约 `ln(10)`。下一对照仅将 token 层数减至 4，维度、耳蜗前端、数据、窗口 CTC 和训练步数保持不变，使用独立目录 `streaming_cochlear_4token_window_s0`。
- 16 层完整 500-step 报告显示内容后期仅升至 `18–19%`，发射始终全 blank。四层轮次因此增加窗口峰值发射损失，直接惩罚允许窗口内没有非 blank 的捷径；同时将可学习耳蜗 FIR 的学习率设为主干的 `0.1×`，其余耳蜗投影仍使用主学习率。新增 `validation_window_peak_probability` 诊断。
- 四层显式发射轮次反向退化为“始终发声”：step 500 内容准确率 `10%`、窗口发射准确率 `10%`、提前发射率 `100%`。下一轮不扩层数：更新门偏置初始化为 `-3`，残差精炼末层零初始化；内容监督覆盖输入末尾 250 ms 至允许发射窗口；blank 权重提高，并增加“窗口峰值比此前峰值至少高 0.5”的相对时序损失与前窗口峰值诊断。该设计仍无离散监听/结束/朗读状态。
- FSDD 预载优化：首次读取与 8→16 kHz 重采样后，按 manifest SHA-256、split、采样率和实际样本 ID 集合写入 Drive 单文件张量缓存；后续训练及新 Colab 会话直接读取缓存，不再逐个访问 1300 个 WAV。manifest、采样率或样本集合变化时自动使用新指纹，不复用失效缓存。
- Stable-timing step 500：窗口前/窗口发声峰值为 `0.0147/0.7194`、提前发射率 `0%`、严格流式差异 `2.38e-6`，说明时机区分已形成；但延迟内容准确率仅 `16.67%`、实际 token 发射召回 `1%`。停止围绕时机调参，判定主要瓶颈为内容归纳、保持与读取。
- S0 主体重构为 `streaming_cochlear_hierarchical_memory_decoder`：两层因果局部编码器先形成音素尺度证据；四层记忆分别读取旧层级状态、较低层归纳与当前证据；独立查询式解码器交叉注意记忆，分别输出数字内容与四段声学重建。发声概率和条件数字分布因子化，时序损失降为辅助项。
- 新训练直接监督输入结束时的内容、随机静音延迟后的内容、两者分布保持，以及从结束记忆重建分段局部声学表征。模型选择优先使用结束/延迟内容准确率，不再以发声指标主导。
- 新增完整时间轴试听审计：分别导出等长、对齐的单声道输入 WAV 与输出 WAV；输入保留前导及结束后静默，输出只在模型实际输出非 blank token 的 tick 生成数字对应提示音。当前尚无波形解码器，因此明确禁止用原音频回放或查表语音冒充模型输出。
- 新增内容记忆审计：报告 25/50/75/100% 输入前缀准确率、0/100/200/300/500/1000 ms 静音保持曲线、全部记忆清除、跨样本记忆打乱、逐层清除、整段输入及末尾 100/200 ms 消融，以及正确/打乱/平均目标声学重建 L1。所有时间点和消融仅用于离线评估，不输入模型。
- 首次审计：前缀准确率随完整度由 `14%/27%/64.67%` 升至 `83.33%`；1000 ms 静音后仍为 `75.67%`；全清除、跨样本打乱与全输入清零分别降至 `10%/9%/9.67%`，语义短时记忆通过。声学重建正确/打乱/平均模板 L1 为 `0.6190/0.8494/0.5166`：虽优于打乱目标，却劣于平均模板，说明声学记忆坍缩到通用表征。审计门槛已拆为 `semantic_memory_pass` 与 `acoustic_memory_pass`，联合结果应为失败。
- 下一轮采用双记忆结构：从已通过语义审计的 `best.pt` 迁移兼容参数，保留四层语义记忆；新增 8 个可学习动态声学槽，每个 tick 通过槽间自注意、当前局部证据和门控递归共同更新，不保存不可变原始 token，也不绑定固定时间片。
- 声学解码器用 8 个查询读取声学槽并重建固定的 `8×32` log-STFT 摘要。损失除正确重建外，同时要求正确目标优于跨样本打乱目标和 batch 平均模板；模型选择也纳入正确相对平均模板的优势。独立目录为 `streaming_dual_semantic_acoustic_memory_s0`。
- 已记录后续感觉—运动发声闭环：输出分为呼吸/喉部声源控制流与构音/声道控制流，经可微声源—滤波执行器生成波形；加入实际执行器感觉、延迟听觉反馈和由控制命令产生的感觉前向预测，使用实际反馈与预测反馈误差进行连续修正。先独立验证控制分解与渲染器，再逐级接入当前记忆，禁止未经约束的自身波形直接回灌。
- M0 已开始实施：新增严格流式双控制自编码器。10 ms 分析器输出 8 维声源/驱动控制与 32 维构音控制；声源显式解释为强度、60–400 Hz 基频、清浊和气声，执行器以连续谐波激励、构音条件与递归运动控制器生成波形。训练同时使用波形、三尺度 STFT、自动相关基频/清浊监督和构音平滑损失。
- M0 验证分别打乱声源流、构音流和使用平均控制；只有正确控制重建同时优于三个对照才通过。独立 notebook 为 `Budgerigar_Sensorimotor_M0.ipynb`，数据继续复用 Drive 持久波形缓存，真实训练和试听只在 Colab 完成。

更新日期：2026-08-05。

## Colab 真实验证记录

2026-08-05 已完成第一轮数据链路：

- 环境：Google Colab，Tesla T4 15 GB。
- Python：3.12.13；PyTorch：2.11.0+cu128；CUDA：12.8。
- 数据来源：CMU ARCTIC 官方 Festvox 归档。
- manifest SHA-256：`15ee6046e8d2dff999b52887c86e9109dd6ae7631a69f5ca2ba4a9588bc6061e`。
- manifest 与运行元数据已保存到用户 Google Drive。
- 首次元数据中的 `git_commit` 为空，已修复自动仓库定位；下次运行需确认 commit 与 `git_dirty` 字段。

该记录证明真实下载、索引、审计和 Drive 持久化链路可运行，不代表模型训练或音频质量已通过验收。

### 特征阶段

2026-08-05 已完成第一轮真实特征提取：

- 环境：Google Colab，Tesla T4 15 GB；PyTorch 2.11.0+cu128，CUDA 12.8。
- Git commit：`b27c837da534600cc226acefac06f2433d8e6b80`。
- 特征 fingerprint：`f1f2ace085a17835`。
- 特征 manifest SHA-256：`35b79ddecd982f782fc528a5034535ef57380fd9d84ec52c76a8c8f971e92afa`。
- 特征 manifest 已保存到用户 Drive。
- 运行记录显示 `git_dirty: true`；已将 editable install 常见生成物 `*.egg-info/`、`build/` 和 `dist/` 加入忽略规则。后续运行仍需检查 `git status --porcelain`，确认没有真实源码改动。

该记录证明 24 kHz log-Mel、能量/VAD、缓存恢复和 Drive 持久化链路已完成真实运行。特征质量的最终确认仍以保存的 `feature_report` 和随机 Mel/VAD 图为依据。

## 环境约束

- 本地仅进行实现、静态检查和无真实数据的轻量单元测试。
- 数据集下载、真实数据审计、GPU 训练、音频质量和实时性能验证均在 Colab 进行。
- 当前本地冒烟测试只证明接口能运行，不代表任何模型或数据阶段已经通过真实验收。

## 已完成

### 阶段 0 基础约束

- 固定内部采样率、模型 tick 与 look-ahead 配置。
- 建立 session 级音频缓冲，支持不规则输入、flush 和 reset。
- `algorithmic_buffer_ms` 明确报告 tick 与 look-ahead 的基础缓冲；完整端到端延迟仍需在模型接入后测量。

### 阶段 1 数据入口

- 实现设计文档所述 JSONL manifest 的严格解析。
- 实现重复 ID、非法 split、缺失音频、WAV 时长偏差检查。
- 实现 `parallel_id` 跨 split 泄漏检测、文本泄漏警告和真人多音色平行组统计。
- 提供 `budgerigar-audit` 命令行入口。
- 实现 CMU ARCTIC 官方归档的幂等下载、SHA-256 校验和安全解压。
- 实现 CMU ARCTIC 与已取得官方 ESD 数据的统一 manifest 适配器。
- 使用稳定哈希按 `parallel_id` 切分，避免同一平行语句跨集合泄漏。
- 提供 [Colab 数据 notebook](../notebooks/Budgerigar_Data.ipynb)，包含许可确认、Drive 缓存、真实审计、报告与运行元数据保存。

### 验证

- 已加入音频分块与 manifest 自动化测试。
- 当前绑定运行时未安装 pytest，因此正式 pytest 套件尚未执行。
- 已通过标准库冒烟测试与 `compileall` 语法检查。
- CMU ARCTIC 适配器已使用合成目录结构完成配对与审计冒烟测试；真实下载仍待 Colab 执行。
- Colab 真实下载、manifest 审计和 Drive 保存已完成；阶段 1 数据链路通过，详细样本统计以 Drive 中 `data_audit.json` 为准。

### 阶段 2 特征入口

- 实现 24 kHz、100 维 log-Mel、10 ms hop 的特征提取。
- 使用 `center=False`，避免特征提取隐式读取整句未来上下文。
- 实现 RMS 增益限制、削波统计、帧能量和相对阈值 VAD。
- 特征配置生成稳定 fingerprint；缓存文件与输出 manifest 绑定该版本。
- 支持 Colab/Drive 中断恢复和原子缓存写入。
- 提供 [特征 notebook](../notebooks/Budgerigar_Features.ipynb)及随机样本可视化。
- Colab 真实特征提取已完成，manifest hash 与 fingerprint 已登记；等待把 `feature_report` 数值纳入质量门槛。

## 下一步

1. 在 Colab 执行特征 notebook，检查特征报告及随机 Mel/VAD 图。
2. 根据真实报告修正异常音频、增益和 VAD 阈值。
3. 在 Colab 运行连续 NeuralEcho smoke training，确认听取阶段的发声强度接近零、复读区间明显上升。
4. 加入多句 episode、句内长停顿和不完整短语，消除固定停顿计时捷径。
5. 在基线成立后实现逐 tick 多层 token 状态和层级探针测试。

## 连续神经复读基线

- 已实现固定目标声线平行配对，目标说话人不会作为输入源。
- 已实现连续双时间轴 episode：输入句期间输出为静默，之后输出目标真人复读。
- 模型没有监听、结束、朗读等离散状态或 action 标签；VAD/能量只作为连续输入证据。
- episode 在完整输入与复读之间加入约 160–280 ms 的轻微变化思考时间；模型不接收倒计时，runtime 不执行定时触发。
- 已提供 [NeuralEcho Colab 训练 notebook](../notebooks/Budgerigar_Neural_Echo_Train.ipynb)。
- 当前 GRU 是验证行为可学性的最小记忆基线；尚未替代为 AutoMachine 式多层 token 状态。
- Colab smoke 默认仅用 64 个配对估计归一化统计、256 个训练配对和 64 个验证配对，避免在 Drive 小文件上进行不必要的全量预处理；该结果不能替代全量训练。

### NeuralEcho smoke 真实运行

2026-08-05 已完成第一轮 NeuralEcho smoke training：

- Colab 环境：Tesla T4 15 GB；PyTorch 2.11.0+cu128，CUDA 12.8。
- Git commit：`08f3a9a9cd4ce528e8b8ad74b09563cbd8f4fb2d`，`git_dirty=false`。
- 行为配置：`continuous_neural_listen_then_repeat`。
- 临时固定目标声线：`arctic_slt`。
- 特征 manifest SHA-256：`35b79ddecd982f782fc528a5034535ef57380fd9d84ec52c76a8c8f971e92afa`。
- 最佳验证损失：`1.263615610175464`。

该结果证明数据配对、连续 episode、GPU 训练、验证与 checkpoint 保存链路已经贯通。单一聚合损失不能证明复读行为成立；进入下一训练阶段前必须完成时间轴行为评估。

下一项评估至少分别报告：听取区间误发声率、思考区间平均发声强度、目标复读区间召回率、预测启动时刻与目标启动时刻偏差，以及输出 Mel 与目标 Mel 的分区损失。

### 时间轴行为评估实现

- 已实现从 `best.pt` 恢复模型并在验证集运行连续 episode。
- 已实现听取/思考误发声率、复读有声召回、全静默率、启动偏差和分区 Mel L1。
- 已保存前 8 个样本的神经发声强度、目标有声轨迹和逐帧 Mel 误差。
- 已提供 [Colab 行为评估 notebook](../notebooks/Budgerigar_Neural_Echo_Evaluate.ipynb)。
- 当前等待 Colab 真实报告；只有 `behavior_pass=true` 才考虑扩大训练，否则优先修正损失、样本时间轴或记忆结构。

### 时间轴行为真实结果

2026-08-05 在 63 个 validation 配对上完成评估，`behavior_pass=true`：

- 评估 commit：`4e0f05274eb512545d85fd94f50009968c15b5e9`，`git_dirty=false`。
- 评估类型：`neural_echo_timeline`。
- 使用 checkpoint：`checkpoints/neural_echo_arctic_slt_f1f2ace085a17835/best.pt`。
- 特征 manifest SHA-256：`35b79ddecd982f782fc528a5034535ef57380fd9d84ec52c76a8c8f971e92afa`。

- 听取平均发声概率：`0.0023767`；误发声率：`0%`。
- 思考区间平均发声概率：`0.0038034`；误发声率：`0%`。
- 复读区间平均发声概率：`0.8734910`。
- 复读有声召回：`98.8209%`。
- 全静默率：`0%`。
- 启动偏差：均值 `2.73` 帧 / `27.30 ms`，中位数 `1` 帧 / `10 ms`。
- 分区归一化 Mel L1：听取 `0.00727`，思考 `0.00846`，复读 `0.66506`。

结论：最小网络已经学会连续的“听取时静默—短暂思考—复读时发声”行为，且无需外部离散状态机。该评估尚未证明输出携带正确的句子内容；下一阶段必须增加正确目标 vs 打乱目标对照、内容检索/语音识别指标和声学样例，排除只生成通用目标声学模式的捷径。

时间轴行为阶段状态：**完成**。

### 内容保持对照评估实现

- 已实现正确目标、随机打乱目标、目标平均帧模板和静默模板 L1 对照。
- 已实现 16 候选正确句子 Top-1 检索与平均排名。
- 已实现输入消融：把源句证据替换为连续静默，测量正确目标误差劣化和输出变化。
- 已提供 [Colab 内容评估 notebook](../notebooks/Budgerigar_Neural_Echo_Content_Evaluate.ipynb)。
- 当前等待 Colab 真实 `content_report.json`；未通过前不扩大训练。

### 内容保持真实结果

第一轮内容评估未通过，`content_pass=false`：

- 正确目标相对打乱目标的 L1 优势：`0.0015603`，低于 `0.02` 门槛。
- 正确目标逐句胜率：`58.73%`，低于 `75%` 门槛。
- 16 候选 Top-1：`7.94%`；随机机会：`6.25%`，接近随机。
- 输入消融劣化：`0.06424`，说明模型并非完全忽略输入。

诊断：当前单一 GRU 隐状态学会了利用输入判断发声时序和粗粒度条件，但没有保存足以区分句子的内容。输出接近目标声线的通用声学模式，而非可靠复读。禁止基于此 checkpoint 扩大训练或宣称内容复读成立。

修正方向：用 AutoMachine 式多层 token 记忆替换单一压缩隐状态；让解码器对 token bank 做内容读取；加入 batch 内正确/打乱目标对比损失和内容检索辅助目标，同时保持连续神经输出、思考间隔以及无离散状态机约束。

### 层级 Token Echo 实现

- 已实现 16 槽固定 token bank；旧 token 每 tick 向深层提供迁入候选。
- 已实现跨槽共享的 GRU token 优化器和层级 embedding。
- 已实现连续可学习写入门；输入 VAD 只是门控证据，不是程序控制。
- 已实现解码 query 对整个 token bank 的注意力读取。
- 已加入静音期间写入强度正则，减少思考/复读时无意义覆盖。
- 已加入 batch 内正确目标 vs 下一个打乱目标的序列 margin loss。
- 已提供 [Colab 层级记忆训练 notebook](../notebooks/Budgerigar_Hierarchical_Echo_Train.ipynb)。
- 当前等待 T4 smoke 结果；训练完成后必须重复时间轴和内容保持两套评估。

### 层级 Token Echo 真实训练

2026-08-05 完成 16-token 层级模型的 200-step T4 smoke：

- Git commit：`46f5a107986a1edfcb8b3ee9cc0056a5cafb5565`，`git_dirty=false`。
- 最佳 validation content loss：`1.33219376206398`，出现在 epoch 2 / step 128。
- 训练 loss 从 epoch 1 的 `1.58076` 降至 step 200 时的约 `1.33181`。
- 后续 validation loss 在 `1.33–1.35` 波动，没有持续改善。
- step 180–200 的对比损失约 `0.144–0.150`，接近 margin `0.15`，尚无证据表明正确目标已明显远离打乱目标。
- 平均静音写入强度降至约 `0.10`，说明写门正学习减少静音覆盖，但仍需探针验证。

训练阶段状态：完成。模型内容能力不按训练 loss 判定，等待时间轴与内容联合评估。

- 已让时间轴和内容评估器同时兼容第一代 GRU 与 `hierarchical_token_echo` checkpoint。
- 已提供 [层级模型联合评估 notebook](../notebooks/Budgerigar_Hierarchical_Echo_Evaluate.ipynb)。

### 层级 Token Echo 联合评估结果

32 个 validation 配对上的联合评估未通过：

- 时间轴行为继续通过：听取/思考误发声率均 `0%`，复读有声召回 `98.89%`，平均启动偏差 `24.38 ms`。
- 内容保持失败：正确目标 L1 `0.68318`，打乱目标 L1 `0.68195`，正确目标反而略差 `-0.00123`。
- 正确目标逐句胜率 `53.13%`；16 候选 Top-1 `3.125%`，低于 `6.25%` 随机机会。
- 目标平均帧模板距离仅 `0.21881`，远低于正确目标距离，证明输出坍缩到通用目标声学模板。
- 输入消融劣化 `0.05266`、输出变化 `0.21493`，说明 token bank 保留了输入相关信息，但这些信息不是可恢复的句子内容。
- `behavior_pass=true`，`content_pass=false`，`combined_pass=false`。

结论：16-token bank 加帧级声学 L1/margin loss 仍不足。禁止扩大同一配置训练。下一轮先把“记忆可恢复文字内容”设为独立门槛：采用更长且多速率更新的 token bank，以 manifest transcript 建立字符/音素 CTC 辅助监督，并用归一化内容 embedding 的 InfoNCE 替代容易梯度抵消的帧级 margin loss。只有内容解码/检索通过后才重新联合声学解码器。

### 内容 Token Memory 预训练实现

- 已建立 30-symbol 英文字符词表：CTC blank、unknown、空格、撇号和 a–z。
- 已按 `parallel_id` 去重，避免同一句不同说话人被 InfoNCE 错当负样本。
- 已过滤 CTC 时间容量不足的样本，并计入连续重复字符所需 blank 路径。
- 已实现 128 槽 token bank、4 帧更新步幅；10 ms hop 下显式槽跨度约 5.12 秒。
- 已实现 token bank → 字符 CTC head。
- 已实现音频记忆 embedding ↔ transcript embedding 双向 InfoNCE。
- 验证指标为字符错误率 CER 与 batch 音频—文本检索 Top-1。
- 已提供 [Colab 内容记忆预训练 notebook](../notebooks/Budgerigar_Content_Memory_Train.ipynb)。
- Smoke 初步门槛：validation CER `< 0.8` 且 batch retrieval Top-1 `> 0.5`；通过后再设计更严格独立候选检索。

### 内容 Token Memory 300-step 结果

- Step 300 / epoch 5：train loss `3.08521`，validation CTC `2.85803`。
- Validation CER `0.78875`，刚刚通过 `< 0.8` 的最低内容恢复门槛。
- Batch=4 retrieval Top-1 `35.94%`；随机机会为 `25%`，已高于随机但未达到 `50%` 门槛。
- `smoke_pass=false`，但与此前低于/接近随机的声学内容检索不同，显式内容目标已产生正向信号。

决策：保持模型和损失配置不变，从 `last.pt` 连同 optimizer/history 断点续训至总计 1000 steps，再判断 CER 和检索是否继续改善。已实现严格配置匹配、optimizer 设备迁移和累计 step/history 恢复；不从零重复前 300 steps。

### 内容记忆续训观察

续训过程中最佳观测点为 epoch 13 / step 812：

- Train loss `2.85043`，validation CTC `2.79719`。
- Validation CER `0.70713`，相对 step 300 的 `0.78875` 持续改善。
- Batch retrieval Top-1 `34.375%`，仍高于 batch=4 的 `25%` 随机机会，但相对 step 300 的 `35.94%` 没有改善。
- `smoke_pass=false`。

诊断：token bank/CTC 已学习部分有序文字内容；检索头停滞主要源于音频 token 和 transcript 都采用平均池化，文字顺序被抹掉。下一版保留 token bank 与 CTC 权重，新增顺序敏感的双向/单向 GRU 序列投影头；旧平均池化 InfoNCE 不再作为继续堆步数的依据。

### 顺序敏感内容检索迁移实现

- 音频侧按旧到新的 token 顺序输入双向 GRU；文本侧按字符顺序输入独立双向 GRU。
- InfoNCE 使用序列 hidden state，而非字符/音频 token 平均值。
- 从旧 `best.pt` 迁移形状兼容的 token optimizer、write gate、level embedding、CTC head 和字符 embedding。
- 新的序列 GRU 与变形后的 projection 不加载旧权重。
- 新 checkpoint 采用 `selection_score = CER - 0.5 × retrieval Top-1` 选择，兼顾解码与检索。
- 已提供 [Colab 顺序敏感迁移 notebook](../notebooks/Budgerigar_Content_Memory_Sequence_Train.ipynb)。
- 初步门槛提高为 validation CER `< 0.7` 且 batch retrieval Top-1 `> 0.5`。

### 顺序敏感迁移首轮结果

- 最佳联合点：epoch 3 / step 192。
- Validation CER `0.70885`，距离 `< 0.7` 门槛仅差 `0.00885`。
- Validation retrieval Top-1 `85.94%`，大幅超过 `50%` 门槛及 batch=4 的 `25%` 随机机会。
- Selection score `0.27916`；`sequence_pass=false` 仅由 CER 略高导致。

结论：顺序敏感 GRU 已解决平均池化检索瓶颈，结构无需再改。下一轮从顺序模型 `best.pt`（而非最后一步）恢复，将学习率降至 `1e-4`、CTC 权重升至 `1.5`、InfoNCE 权重降至 `0.5`，定向微调至总 step 600；恢复后显式覆盖 optimizer 学习率。

### 顺序内容模型 600-step 定向微调结果

- 微调确实运行至 step 600；不存在结果单元误判训练未执行的问题。
- 最低 validation CER 为 step 320 的 `0.701646`，未达到 `< 0.7` 门槛。
- Step 256–600 的 CER 在约 `0.7016–0.7154` 之间震荡，已形成平台期。
- Retrieval Top-1 全程稳定在约 `79.69–85.94%`，顺序检索能力保持良好。
- 最终 step 600：validation CTC `2.76598`、CER `0.70748`、retrieval `84.375%`。

诊断：序列检索头已成功，但它可能同时利用句长、韵律和整体声学形状等句子指纹；70% 左右 CER 仍不足以证明可恢复文字。当前输入仅经线性 Mel 投影便进入 token optimizer，局部音素表征能力不足。停止继续微调同一结构。

下一结构增加因果局部声学内容编码器，并使用双 CTC：第一项直接约束下采样声学序列可恢复字符，第二项约束最终 token bank 仍保留这些字符；InfoNCE 继续作为辅助而非替代内容解码。训练样本扩大到全部不重复 `parallel_id`，避免 256 条小样本限制。

### 局部声学编码与受控扩参

- 已加入 4 层严格因果的 depthwise temporal convolution + 前馈残差编码器；每帧只依赖当前及历史输入。
- hidden dimension 从 128 增至 192，token slots 从 128 增至 160。扩参集中在局部语音辨识和长程内容容量，而非扩大声学生成器。
- 新增 acoustic CTC head，与 token-bank CTC 构成双重内容约束；顺序 InfoNCE 权重降至 0.25。
- smoke training 使用全部不重复 `parallel_id`，不再限制为 256/64 条；新结构独立从头训练，避免把旧结构 plateau 的 optimizer 状态带入。
- 新门槛为 bank CER `<0.65`、acoustic CER `<0.65`、retrieval Top-1 `>0.5`。未通过前不接回声学复读解码器，也不继续扩大参数。
- Colab 入口：`notebooks/Budgerigar_Content_Local_Dual_CTC_Train.ipynb`。

### 双 CTC 400-step 结果与定向续训

- Epoch 2 / step 400：acoustic CER `0.57476`，已通过 `<0.65`；retrieval Top-1 `93.75%`，显著通过 `>0.5`。
- Token-bank CER `0.79835`，未通过 `<0.65`；`dual_ctc_pass=false`。
- 模型共 `1,988,490` 参数，T4 已可训练。局部编码与检索均已形成强信号，当前没有扩大参数的证据。
- 失败位置已收窄到“局部声学序列写入长程 token bank 后，字符可恢复性下降”；不是原始声学表征不足。
- 因完整数据下 400 steps 仅约 2 epochs，先从 `last.pt` 续训至总 step 1200：学习率 `1e-4`，bank CTC 权重 `2.0`，acoustic CTC 权重 `0.5`，InfoNCE 权重 `0.1`。
- 续训使用 `last.pt` 保留完整 optimizer/history；不从 `best.pt` 重开，也不重复前 400 steps。若 step 1200 时 bank CER 仍高于 `0.70`，再修改写入/记忆结构。

### 双 CTC 2000-step 复核：判定平台期

- 最佳点 epoch 9 / step 1962：bank CER `0.68964`、acoustic CER `0.50720`、retrieval Top-1 `93.75%`。
- 相比 step 1162 的 bank CER `0.69616`，800 steps 仅变化 `0.00652`；在没有重复随机种子、置信区间和逐句配对检验时，不能将其解释为可靠优化。
- 两轮训练修改过损失权重，因此 train loss `6.35 → 7.04` 不具备横向比较意义。
- 更重要的是，CTC 与 batch retrieval 都是辅助探针，尚未生成并评估复读音频；这些结果不能证明整体鹦鹉复读模型得到改善。
- 撤回“继续堆到 2000 步即可收敛”的判断。该结构标记为 plateau，Colab 单元改为只读历史报告，防止继续消耗 T4。
- 下一结构必须分离两种职责：局部内容序列保存可重放的时间细节；AutoMachine 多层 token optimizer 保存跨较长时间提炼的抽象内容。解码器同时读取二者，而不是强迫抽象 token bank 自身充当逐字符时间轴。
- 下一轮验证必须包含实际复读输出的正确目标/打乱目标距离、内容检索、静音与启动时序，并加入“移除局部内容路径”和“移除抽象 token 路径”两项消融。只有端到端内容指标改善才算整体优化。

### 双路径端到端复读实现

- 新增局部软写入序列：每 4 帧提出一次写入，写入强度由连续神经门根据声学表示与 VAD 证据决定；不存在程序化的结束检测。
- 保留 AutoMachine 式抽象 token bank：共享 GRU optimizer、层级 embedding 与连续写门持续提炼信息，承担超过局部序列显式跨度的长期内容。
- 因果局部编码器及兼容的 optimizer/write-gate 权重从双 CTC `best.pt` 迁移；新解码查询、双路注意力、人格向量和声学头重新学习。
- 解码器每个 tick 同时注意局部与抽象记忆，输出目标声线 Mel 和发声概率；听、思考、复读仍由连续时间轴监督自然形成。
- 验证不再以 CTC 作为最终通行证：同一 checkpoint 必须通过时间轴行为、正确/打乱目标内容评估，并且移除任一路径都应使真实复读 L1 恶化。
- Colab 入口：`notebooks/Budgerigar_Dual_Path_Echo_Train.ipynb`；首轮仅 200 steps、batch 2，未联合通过前禁止扩大训练。

### 双路径首轮联合评估

- `behavior_pass=true`、`ablation_pass=true`，但 `content_pass=false`、`combined_pass=false`。
- 完整输出 repeat L1 `0.67967`；移除局部路径恶化 `0.12436`，移除抽象路径仅恶化 `0.00860`。
- 结论：模型会使用局部路径，但当前无序软 FIFO 与自由注意力不能可靠控制复读顺序；这不构成整体优化。
- 停止扩大训练与参数，下一阶段按 [连续双时钟与单调复读指针](10_连续双时钟与单调复读指针.md) 重构。
- 设计澄清：有序事件带承担精确时间轴；AutoMachine token 层承担逐层形成的整体理解。后层 token 是在旧理解上持续吸收前层提炼证据的递归状态，不对应单个时间片、固定 tick 窗口或单个单词。
- 已实现第一版连续事件坐标、4 层整体理解 optimizer、自下而上证据/自上而下语境融合、连续 readiness 与非负单调读指针；Colab 入口为 `notebooks/Budgerigar_Monotonic_Understanding_Train.ipynb`。
- 首轮 200-step 联合评估仍失败：正确目标 L1 `0.69024`、打乱目标 `0.68823`、检索 Top-1 `3.125%`（随机 `6.25%`）；输出仍是平均模板。两路径消融均产生约 `0.02` 恶化，只证明路径被使用，不证明读取顺序正确。
- 下一轮不扩参：从首轮 `best.pt` 迁移，在训练期用累计输入 VAD 约束连续写时钟、用累计目标发声量约束连续读时钟；推理仍完全由网络状态决定。独立输出目录为 `monotonic_clock_aligned_*`。
- 时钟实验在 step 80 已显示 clock loss 可下降而 acoustic/contrastive 无同步改善，因此停止该运行。
- 理解层改为递归层间注意力：每层以上一 tick 自身理解为 query，读取上一 tick 全部层与本 tick 已更新浅层，然后由本层 optimizer 修订自身；层不绑定时间片或单词。新实验目录为 `monotonic_recursive_attention_*`。
- 递归注意力训练至 step 100 仍无改善：acoustic 后段约 `1.20–1.25`，contrastive 仍围绕 `0.15`，因此停止。递归理解本身不足以解决平均模板，瓶颈转向非自回归声学解码。
- 下一轮保留递归理解并加入上一帧 Mel feedback；teacher forcing 从 `0.8` 线性退火到 `0.2`，验证/推理固定为 `0`。独立目录 `monotonic_recursive_feedback_*`，仍限制 200 steps。



## 当前不包含

- 尚未下载数据集，也没有把未经确认许可的数据加入仓库。
- 尚未生成或训练模型 checkpoint。
- 尚未实现声码器、实时麦克风输出、Colab notebook 或 Gradio 页面。
